import math
import random
kolmenumeroinen = "".join(str(random.randint(0,9)) for x in range(3))
nelinumeroinen = "".join(str(random.randint(1,6)) for x in range(4))

print("Kolemnumeroinen koodi: ", kolmenumeroinen)
print("nelinumeroinen koodi: ", nelinumeroinen)
